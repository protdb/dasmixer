# v0.7.0a2

**Date:** 2026-07-13

---

## Bug Fixes

### Unlocalized PTM (ProForma `?` notation) Crash & Infinite Loop

**Problem:** Identifications with unlocalized modifications (e.g., `[Deamidated]?FQNALLVR`) caused `Fragmenter` from `peptacular` to throw an exception because it only supports positioned ProForma. The exception was silently swallowed (worker logger sent to no-op logger when `log_to_file=False`), and `intensity_coverage=None` was written to DB. On the next run with `only_missing=True`, the same rows were re-read at offset=0, creating an infinite loop.

**Fix — `seqfixer.py`:** New function `_expand_unlocalized()` — extracts `unlocalized_modifications` from ProForma params, finds valid attachment sites per `ptm_list`, generates all positioned variants using `combinations` (for duplicate mods) + `product` (across different mod groups). **Guard** (`_MAX_UNLOCALIZED_COMBOS=256`): when variant count exceeds limit, each group's site list is reduced to `[first, last]` to keep combos tractable. Falls back to canonical sequence when no valid site exists.

**Fix — `identification_processor.py`:** `process_single_ident()` now detects `'?' in sequence`, calls `_expand_unlocalized()`, runs `match_predictions` for each variant, and picks the winner by `intensity_percent`. Original `ppm`/`theor_mass` (correctly calculated by pyteomics proforma) are preserved.

**Fix — Exception handler robustness:** `intensity_coverage: None` changed to `intensity_coverage: 0` — failed rows are written as processed (0% coverage) rather than unprocessed (`NULL`), preventing infinite re-processing. Added unconditional `print(..., file=sys.stderr, flush=True)` fallback to make errors visible regardless of logger config.

### Schema — `protein_quantification_result` columns mismatch

**Problem:** The `CREATE TABLE` statement in `schema.py` still used the old column name `abs_value REAL`, while migration 0.7.0 renames it to `abs_value_mol REAL` and adds `abs_value_gl REAL`. A project created from scratch (without migration path) would have the wrong schema, causing `add_protein_quantifications_batch` to fail with `no such column: abs_value_mol`.

**Fix:** Updated `schema.py` CREATE TABLE to match the post-migration state: `abs_value REAL` → `abs_value_mol REAL` + `abs_value_gl REAL`.

### MGF Export — all_params, intensity, charge array, SCANS, header comment

**Problem:** MGF export dropped most metadata from the original spectrum:
- No `INSTRUMENT`, `RTINSECONDS`, `SCANS`, or any fields from `all_params` were written
- `PEPMASS` was written without precursor intensity (`PEPMASS=<mass>` instead of `PEPMASS=<mass> <intensity>`)
- Peak charge column (third column) was always absent
- No file origin comment
- Spectra were sorted by internal DB `id` instead of original file order (`seq_no`)

**Fix — `mgf_export.py`:** Completely rewrote `_build_spectrum_params()`:

1. **`all_params` support:** All "extra" fields from the original MGF header (`instrument`, `rtinseconds`, etc.) are now preserved in the export. Internal pyteomics keys (`_spectrum_type`, `mass`) and explicitly-handled keys (`charge`, `pepmass`, `title`, `scans`, `seq`) are filtered out.
2. **`PEPMASS` with intensity:** Now writes both mass and precursor intensity: `PEPMASS=<mass> <intensity>`. Intensity comes from `spec_row.intensity` (stored during import from `pepmass[1]`).
3. **`charge`:** Taken from `spec_row.charge` (INTEGER), not from `all_params`. Respects `write_spectra_charge` override from identification.
4. **`SCANS` fallback:** If `scans` is not present in `all_params`, falls back to `spec_row.scans` from the database (critical for PLGS spectra where SCANS is only in the TITLE).
5. **`SEQ` with `write_seq`:** Overwrites from identification when flag is set; preserves original `all_params.seq` when not.
6. **Peak charge array:** Reconstructs from `charge_array_common_value` using `np.full()` when no per-peak array is stored, restoring the third column (e.g., `537.3241 45.0 1.0`).
7. **File header comment:** Each exported MGF file starts with `# Exported from DASMixer`.
8. **Peak charge as integer:** Charge values in the third peak column are written as integers (`1`), not floats (`1.0`). Handles both `charge_array_common_value` and per-peak BLOB storage.

---

## Files Changed

### Core (dasmixer-core)
| File | Change |
|---|---|
| `src/dasmixer/api/calculations/ppm/seqfixer.py` | Added `_expand_unlocalized()` function and `_MAX_UNLOCALIZED_COMBOS` constant |
### Core (dasmixer-core)
| File | Change |
|---|---|
| `src/dasmixer/api/calculations/ppm/seqfixer.py` | Added `_expand_unlocalized()` function and `_MAX_UNLOCALIZED_COMBOS` constant |
| `src/dasmixer/api/calculations/spectra/identification_processor.py` | `process_single_ident()` — unlocalized PTM expansion branch; `except Exception` — `intensity_coverage=0` + stderr fallback |
| `src/dasmixer/api/project/schema.py` | `abs_value REAL` → `abs_value_mol REAL` + `abs_value_gl REAL` in `protein_quantification_result` CREATE TABLE |
| `src/dasmixer/api/export/mgf_export.py` | Rewritten `_build_spectrum_params()` — `all_params` passthrough, PEPMASS+intensity, SCANS fallback, charge array reconstruction, `# Exported from DASMixer` header comment; `_get_spectrum_ids()` — sort by `seq_no` instead of `id` |