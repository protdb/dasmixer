# v0.7.2a3

**Date:** 2026-09-02

---

## New Features

### Subplot заголовки — настраиваемые поля (Peptide Ion Plot)

Полностью переработана логика формирования заголовков subplot'ов в графике ионов. Пользователь теперь может выбирать, какие параметры отображаются в заголовке каждого subplot'а, через строку чекбоксов **Subplot Header Fields** в панели Plot Settings.

**Список полей и их состояние по умолчанию:**

| Поле | По умолчанию | Отображение |
|---|---|---|
| Preferred | ☑ | `★` (префикс) |
| Tool | ☑ | Название инструмента |
| Sequence | ☑ | Последовательность |
| Protein ID | ☐ | ID белка |
| Gene | ☐ | `Gene: {value}` |
| Identity | ☑ | `Identity: {value:.2f}` |
| PPM | ☐ | `PPM: {value:.2f}` |
| Score | ☐ | `Score: {value}` |
| Intensity Coverage | ☐ | `iC: {value:.2f}` |
| Quality | ☐ | `Q: {value:.2f}` |
| LCRR | ☐ | `LCRR: {value:.2f}` |

**Особенности:**
- Если значение поля `None` (отсутствует), оно не отображается в заголовке, даже если соответствующий чекбокс включён.
- Quality/LCRR/intensity_coverage не рассчитываются для matched_sequence при неполном совпадении в белке (`identity < 1.0`) — для таких строк эти поля всегда `None` и не выводятся.
- Gene подтягивается из таблицы `protein` через join по `protein_id` (доступен только для matched-строк; для исходной identification — `None`).
- Определён единый источник истины `HEADER_FIELD_OPTIONS` в `plot_mixin.py` — GUI-код строит чекбоксы динамически, что исключает рассинхронизацию списка полей между core и gui (поля/лейблы/дефолты заданы в одном месте).

---

## Notes

- `PROJECT_VERSION` / `APP_VERSION` **не повышаются** — управляется разработчиком при сборке.
- Миграция БД не требуется — все используемые поля (`quality`, `lcrr`, `intensity_coverage`, `gene`, `matched_coverage_percent`) уже существуют в схеме.

---

## Version Changes

| Constant | Old | New |
|---|---|---|
| `APP_VERSION` | `0.7.2a2` | `0.7.2a3` (разработчик) |
| `PROJECT_VERSION` | `0.7.2` | `0.7.2` (unchanged) |
| `MIN_SUPPORTED_PROJECT_VERSION` | `0.1.0` | `0.1.0` (unchanged) |

---

## Files Changed

### Core (dasmixer-core)
| File | Change |
|---|---|
| `src/dasmixer/api/project/mixins/plot_mixin.py` | `HEADER_FIELD_OPTIONS` + `DEFAULT_HEADER_FIELDS` — единый источник истины; `_format_spectrum_plot_header` — новая функция формирования заголовка по выбранным полям с пропуском `None`; `get_spectrum_plot_data` — новый параметр `header_fields`, доработаны SQL-запросы (добавлены `i.quality`, `i.lcrr`, `i.intensity_coverage`, `m.matched_coverage_percent`, join к `protein` для `gene`), expanded plot dicts |

### GUI (dasmixer-gui)
| File | Change |
|---|---|
| `src/dasmixer/gui/views/tabs/peptides/peptide_ion_plot_view.py` | Импорт `HEADER_FIELD_OPTIONS`/`DEFAULT_HEADER_FIELDS` из core; `header_fields` в `get_default_settings`; динамические чекбоксы в `_build_plot_settings_view`; сбор значений в `_update_settings_from_ui`; проброс `header_fields` в `get_spectrum_plot_data` |

### Docs
| File | Change |
|---|---|
| `docs/project/changes/v0.7.2a3.md` | NEW — changelog (текущий файл) |
| `docs/project/spec/0.7.2a3_REQUIREMENTS.md` | NEW — requirements (ранее создан) |
