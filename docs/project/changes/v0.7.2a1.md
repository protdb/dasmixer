# v0.7.2a1

**Date:** 2026-09-01

---

## New Features

### Longest Consecutive Run Ratio (LCRR)

- Показатель **LCRR** (Longest Consecutive Run Ratio, 0..1) теперь сохраняется в БД (`identification.lcrr`) при расчёте Ion Coverage. Расчёт — на основе функции `calculate_longest_consec_run_ratio` в `quality_calculations.py`, значение уже возвращается полем `MatchResult.longest_consec_run_rate` из `match_predictions` (реализация — в 0.7.1).
- **LCRR** — отношение длины максимальной непрерывной последовательности совпавших ионов одного типа к длине пептида. Косвенный показатель качества спектра: чем ближе к 1, тем непрерывнее подтверждение теоретическими ионами.
- **Min LCRR** (по умолчанию 0.2) — новый параметр Preferred settings инструмента. Фильтрует кандидатов в Select Preferred: идентификации с `lcrr < Min LCRR` (или без вычисленного LCRR) не выбираются preferred через normal-пул; trusted-пул (`Trust all identifications`) игнорирует этот порог, как и все остальные.
- При Clear Calculations поле `lcrr` сбрасывается в NULL наравне с остальными расчётными полями.

### Peptide Table

- Новая колонка **LCRR** видна по умолчанию в таблице Peptide Identifications (после Quality). Для необсчитанных идентификаций отображается пустое значение. Колонку можно скрыть/показать через стандартный gear-диалог выбора столбцов.

---

## Version Changes

| Constant | Old | New |
|---|---|---|
| `APP_VERSION` | `0.7.1a4.dev1` | `0.7.2a1` |
| `PROJECT_VERSION` | `0.7.1` | `0.7.2` |
| `MIN_SUPPORTED_PROJECT_VERSION` | `0.1.0` | `0.1.0` (unchanged) |

---

## Database Schema Migration (0.7.1 → 0.7.2)

- `identification` → новый столбец `lcrr REAL`

---

## Files Changed

### Core (dasmixer-core)
| File | Change |
|---|---|
| `src/dasmixer/versions.py` | `PROJECT_VERSION = "0.7.2"`, `APP_VERSION = "0.7.2a1"` |
| `src/dasmixer/api/project/schema.py` | `identification`: добавлен `lcrr REAL` |
| `src/dasmixer/api/project/migrations.py` | Миграция 0.7.2 (ALTER TABLE identification ADD COLUMN lcrr REAL) |
| `src/dasmixer/api/project/mixins/identification_mixin.py` | `put_identification_data_batch` — запись `lcrr`; `clear_calculations` — сброс `lcrr = NULL`; `get_idents_for_preferred` — параметр `min_lcrr` + SQL WHERE; `get_all_idents_for_preferred` — `lcrr` в SELECT |
| `src/dasmixer/api/project/mixins/peptide_mixin.py` | `get_joined_peptide_data`/`count_joined_peptide_data` — `lcrr` в SELECT |
| `src/dasmixer/api/calculations/spectra/identification_processor.py` | `process_single_ident` — `longest_consec_run_rate` в оба return-словаря; `process_identificatons_batch` — `longest_consec_run_rate: None` в error-fallback |
| `src/dasmixer/api/calculations/peptides/matching.py` | `calculate_preferred_identifications_for_file` — чтение `min_lcrr` из `tool_params` и проброс в `get_idents_for_preferred` |

### GUI (dasmixer-gui)
| File | Change |
|---|---|
| `src/dasmixer/gui/views/tabs/peptides/tool_settings_section.py` | Контрол `Min LCRR` (default 0.2) в Block 2 Preferred settings; валидация [0, 1]; сериализация в `save_tool_settings`/`get_tool_settings_for_matching` |
| `src/dasmixer/gui/views/tabs/peptides/peptide_ion_table_view.py` | `header_name_mapping['lcrr'] = 'LCRR'`; `'lcrr'` в `default_columns` |

### Docs
| File | Change |
|---|---|
| `docs/project/changes/v0.7.2a1.md` | NEW — changelog |
| `docs/project/spec/0.7.2a1_SPEC.md` | NEW — спецификация реализации |