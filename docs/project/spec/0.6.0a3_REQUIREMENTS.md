# 0.6.0a3 Мелкие и не очень доработки

## Исправления:

Ошибка при запуске: `[Logging] Failed to configure logging: name 'Path' is not defined`, связана с глобальной конфигурацией, более детальный стектрейс (при сохранении настроек):

```
exception calling callback for <Future at 0x728d82b08510 state=finished raised NameError>
Traceback (most recent call last):
  File "/usr/lib/python3.14/concurrent/futures/_base.py", line 334, in _invoke_callbacks
    callback(self)
    ~~~~~~~~^^^^^^
  File "/home/gluck/.cache/pypoetry/virtualenvs/dasmixer-rt7LtDi0-py3.14/lib/python3.14/site-packages/flet/controls/page.py", line 504, in _on_completion
    raise exception
  File "/home/gluck/PycharmProjects/dasmixer/dasmixer-gui/src/dasmixer/gui/views/settings_view.py", line 455, in _save_settings
    _apply_logging_config(config)
    ~~~~~~~~~~~~~~~~~~~~~^^^^^^^^
  File "/home/gluck/PycharmProjects/dasmixer/dasmixer-gui/src/dasmixer/gui/views/settings_view.py", line 33, in _apply_logging_config
    else Path.home() / ".cache" / "dasmixer" / "logs"
         ^^^^
NameError: name 'Path' is not defined
```

## Обновления CLI

Это давно было нужно сделать, давай сделаем:
- Создание инструмента `dasmixer proj.dasmix tool add --name PN2 --parser PowerNovo2`
- Конфигурация инструмента `dasmixer proj.dasmix tool configure --name PN2 --key X --value Y`
- Список параметров конфигурации: `dasmixer proj.dasmix tool param-list`
- Список инструментов: `dasmixer proj.dasmix tool list`
- Конфигурация проекта `dasmixer proj.dasmix project configure --key X --value Y`
- Имплементация импорта идентификаций (сейчас там заглушки и их нужно пересмотреть)
- Импорт FASTA
- Все основные операции пайплайна:
  - `dasmixer proj.dasmix calculate peptides` (аналогично кнопке calculate peptides в GUI)
  - `dasmixer proj.dasmix calculate ion-coverage`
  - `dasmixer proj.dasmix calculate preferred`
  - `dasmixer proj.dasmix calculate peptide-match`
  - `dasmixer proj.dasmix calculate protein-idents`
  - `dasmixer proj.dasmix calculate lfq --empai --ibaq --nsaf --top3` (наличие флага подразумевает, что вычислеяется в том числе и этот метод lfq)

## Дополнительные кнопки на главной

Под логотипом на главной нужно добавить две кнопки (в одном ряду), дублирующие функционал главного меню:
- New project
- Open Project

## Исправление модальных окон в manage samples view

Ошибка по кнопке удаления образца (конкретного образца, удаление по списку работает корректно): `The application encountered an error: SampleViewPanel(6163) Control must be added to the page first`

Вероятно, проблема на этой странице есть и с другими окнами