# Этап 13, мелкие доработки и улучшения:

## Доработки экспорта данных

Нужен модальный диалог для экспорта данных (всех: из таблиц; сохранение графиков; экспорт отчетов и экспорт в Word со вкладки Plots).
UI блокируется, показывается loader, там где происходит экспорт в несколько этапов или создаются сразу несколько файлов, отображается изменение статуса.

## Вкладка Plots

Добавить кнопку "Select All" для выбора всех ихображений (просто проставляет у всех чекбоксы)

## Окно настроек

Настройки Batch из меню "настройки" не применяются к реальным размерам батчей. Нужно поправить, связать параметры в настройках к функциям.
Нюанс: весь проброс параметров должен идти через GUI, обращение к конфигу внутри модулей dasmixer/api/calculations нежелательно

Добавить настройку Max CPU threads, выполняемых с помощью Multiprocessing. По умолчанию определяется как сейчас (os.cpu_count - 1)

Добавить настройки логирования: флаг "Log operations to file", log level (dropdown: DEBUG, INFO, WARNING, ERROR), path to logs folder.  Привязать настройки к системному логгеру.

## Samples tab

### Диалог импорта (Pattern Matching)

Список файлов при импорте не пролистывается. Нужно иметь возможность пролистать файлы. Также нужно к каждому добавить чекбокс и сделать возможность исключить из импорта (по умолчанию импортируются все). Также нужна возможность при этом переписать ID образца вручную.

Кнопка Import должна быть активной только после Preview files, если для всех выделенных файлов определен Sample ID

Относится как к импорту MGF, так и к импорту Identifications

Для полей File pattern и Sample ID Pattern нужно добавить всплывающие подсказки по наведению на знак вопроса в кружке.

## Peptides tab

### Диалог загрузки белковых последовательностей

Секция Protein Sequence Library: Удалить вообще флаг "Enrich Data from UniProt".

Загрузку файла FASTA убрать в модельный диалог.

На самой форме должны остаться:
- Кнопка Load FASTA (открывает модальное окно)
- Кол-во белков в таблице Proteins на данный момент
- Блок Protein Mapping Settings.

### Перемещение кнопок

В секции Actions убрать кнопки из выпадающего блока, сделать их положение фиксированным.
Убрать дубликаты этих кнопок из соседних секций. Переместить Selection Criterion из Preferred identification Selection вниз секции Ion Matching Settings.

Оставить, получается, два вертикальных блока в этом разделе:
1) Ion Matching settings
2) Actions + Protein sequence Library

В Actions переименовать кнопку Run Identification Matching в Select Preferred (с иконкой ft.Icons.STAR_OUTLINE)

Добавить в Actions кнопку Save settings: по кнопке должны сохраняться указанные пользователем параметры идентификации в момент нажатия кнопки (логика сохранения при запуске остается)

Добавить "сворачивание" блока Tool Settings при переходе на другую вкладку. Вопрос производительности, насыщенный контролами блок. При сворачивании: сохраняем параметры (фоном), удаляем секцию из Flet, см. логику для Table And Plot View.

### Sequence selection criteria

Этот выпадающий блок сейчас не работает корректно. Нужно пройти всю цепочку и посмотреть, как правильно должны называться в нём параметры. Сейчас там заглушка под один вариант, нужно починить.

## Reports tab

Доработать диалоговое окно Parameters, уйти от фиксированного размера с растягиванием по высоте, делать размер окна по числу компонентов

Если контролов нет, делать кнопку Parameters неактивной